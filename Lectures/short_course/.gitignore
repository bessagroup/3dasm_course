# The three demo folders are written by aescape_course.ipynb and are never
# committed. With a Claude subscription the notebook rebuilds all of them and
# really calls the agent; without one, read the notebook's stored outputs
# rather than running it.
demo1_car_no_agents/
demo2_car_one_agent/
demo3_car_multi_agent/
